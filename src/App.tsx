import * as React from "react";
import CryptoMarketContainer from './container/CryptoMarketContainer';

export default class App extends React.Component {
  public render() {
    return (
      <CryptoMarketContainer/>
    );
  }
}
